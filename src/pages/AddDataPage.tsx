import { TableDataForm } from "../components/Forms/TableDataForm"

export const AddDataPage = () => {
    return (
        <div>
            <h2 className="text-center mb-2 font-semibold">Add new data to table</h2>
            <TableDataForm />
        </div>
    )
}